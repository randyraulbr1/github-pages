# DEPRECATED — Render suspendido (5 GB/mes). Rollback solo emergencia.
# Producción: Oracle Cloud — ver docs/ORACLE_MIGRACION.md
name: Render warmup (legacy)

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -sfS --max-time 30 https://mariel-online.onrender.com/health
          echo " OK — $(date -u +%Y-%m-%dT%H:%M:%SZ)"
